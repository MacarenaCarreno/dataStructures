var removeNthFromEnd = function(head, n) {
  console.log(head);
  
};

let ListNode = {
  val: 1,
  next: {
    val: 2,
    next: {
      val: 3,
      next: {
        val: 4,
        next: {
          val: 5,
          next: null,
        },
      },
    },
  },
};

removeNthFromEnd(ListNode, 2);
//console.log(ListNode);
